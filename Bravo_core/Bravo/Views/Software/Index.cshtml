@using Bravo.Models.ViewModels.Software

@model SoftwareIndexViewModel

@{
    ViewData["Title"] = "Geräte";
}

<h1>@ViewData["Title"]</h1>

<div>


    <table class="table" id="brave-toolTable">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th></th>
                <th><a class="btn fa fa-plus" asp-area="" asp-controller="Software" asp-action="Edit" asp-route-id="0"></a></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.SoftwareList)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Bezeichnung</td>
                    <td><a class="btn fa fa-pencil-square" asp-area="" asp-controller="Software" asp-action="Edit" asp-route-id="@item.Id"></a></td>
                    <td></td>
                </tr>
            }
        </tbody>
    </table>
</div>   